<body>
    <head>
        <link rel="stylesheet" href="/styles.css">
        <h1>Homepage</h1>
    </head>
    <nav>
        <ul>
            <% for (let i = 0; i < messages.length; i++) { %>
                <h3>Message <%=[i]%></h3>
                <li>
                    <p id="text"><%= messages[i].text %></p>
                    <p id="user"><%= messages[i].user %></p>
                    <p id="date"><%= messages[i].added %></p>
                    
                    <a href="/message/<%=[i]%>"  id=<%=[i]%>><button>Open</button></a>
                </li>
           <% } %>
        </ul>
        <a href="/new">
            <span>Create new User ==></span>
        </a>
    </nav>
</body>